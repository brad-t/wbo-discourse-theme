hide_topic_footer_controls:
  default: true
  description: "Hides the reply/bookmark/share buttons at the bottom of each topic"

wbo_site_base_url:
  default: "https://worldbeyblade.org"
  description: "Base URL of the WBO WordPress site. Used by the user menu to link to My Tournaments and fetch per-user counts from /wp-json/wbo/v1/user-summary."
